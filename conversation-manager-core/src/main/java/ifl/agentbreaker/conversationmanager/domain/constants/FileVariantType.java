package ifl.agentbreaker.conversationmanager.domain.constants;

/** Server-owned purpose of a sanitized file derivative. */
public enum FileVariantType
{
    /** Bounded, metadata-free image supplied to the model and persisted preview UI. */
    MODEL_INPUT
}
